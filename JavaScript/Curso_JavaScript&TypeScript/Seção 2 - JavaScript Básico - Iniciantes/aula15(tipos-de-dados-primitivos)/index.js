// string, number, undefined, null, boolean

const nome = 'Víctor'; // string --> ASPAS SIMPLES
const nome1 = 'Víctor'; // string --> ASPAS DUPLAS (Alterei a configuração do Prettier para usar somente aspas simples)
const nome2 = `Víctor`; // string --> TEMPLATE STRING
const num = 10; // number
const num1 = 10.52; // number
let nomeAluno; // undefined --> não aponta para local nenhum na memória
const sobrenomeAluno = null; // null --> não aponta para local nenhum na memória
const aprovado = false; // boolean --> true or false (lógico)

/* NÃO FAZ PARTE DA AULA!

Sinal de igual (=) é chamado de operador de atribuição

const a = [1, 2];
const b = a;
console.log(a, b);

b.push(3);
console.log(a, b); 

let c = 2;
const d = c;
console.log(c, d); // 2, 2

c = 3;
console.log(c, d); // 3, 2 */
