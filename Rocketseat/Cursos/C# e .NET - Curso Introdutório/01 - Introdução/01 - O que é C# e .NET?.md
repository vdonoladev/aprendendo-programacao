**C# (lê-se “C sharp”)** é uma **linguagem de programação** criada pela Microsoft lá no começo dos anos 2000, como parte do projeto .NET.

Ela é **fortemente tipada, orientada a objetos** e tem uma sintaxe parecida com C, C++ e Java — o que facilita a vida de quem já veio dessas linguagens.

C# é usada pra criar de tudo: **aplicações web, desktop, jogos (com Unity), APIs, apps mobile (com Xamarin/Maui)** e até microserviços em nuvem.

Agora, o **.NET** é o **ecossistema/plataforma** onde o C# (e outras linguagens, tipo F# e VB.NET) rodam.

Ele fornece o motor por trás da execução do código e uma tonelada de ferramentas, bibliotecas e frameworks que facilitam a vida do dev.

Vamos quebrar o .NET em partes:

1. **.NET Runtime (CLR – Common Language Runtime)**

   É tipo a “máquina virtual” do .NET — o ambiente que compila e executa teu código C#.

   Ele faz gerenciamento de memória, garbage collection, segurança, etc.

   Pensa como o JVM faz pro Java.

2. **.NET Class Library**

   Um conjunto enorme de bibliotecas prontas pra usar — manipulação de arquivos, rede, criptografia, JSON, XML, banco de dados, e por aí vai.

3. **ASP.NET / ASP.NET Core**

   O framework web dentro do .NET. É o que usamos pra criar **APIs REST, sites, e serviços backend** de alta performance.

   O “Core” é a versão moderna, open-source, e **multiplataforma** (roda em Windows, Linux e macOS).

4. **Ferramentas e SDKs**

   Tem o **Visual Studio** e o **Visual Studio Code**, com extensões e templates que tornam o desenvolvimento bem produtivo.

Resumindo o esquema:

👉 **C#** é a **linguagem**.

👉 **.NET** é a **plataforma** onde ela roda.

Pensa no .NET como o “motor e a carroceria”, e no C# como o “motorista” que manda o carro andar.
